export type ISupplier = {
    id: string;
    code: number | string
    name: string
    phone: string | number;
    email: string | number;
    note: string;
    address: string;
    region: string;
    status: string;
    branch: string;
}